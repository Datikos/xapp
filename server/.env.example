# Get a free key at https://twelvedata.com/pricing
TWELVE_DATA_API_KEY=

# Optional: change if port 3000 is already taken locally.
# If you change this, also update the proxy target in frontend/proxy.conf.json.
PORT=3000
